// src/utils/constants.js

// Dimensiones de la mesa de trabajo
export const CANVAS_WIDTH = 1080;
export const CANVAS_HEIGHT = 720;

// Configuración de la cuadricula
export const GRID_CELL_SIZE = 20;
export const GRID_STROKE_COLOR = "#ddd";
export const GRID_DASH_PATTERN = [2, 2];

// Colores y estilos por defecto para shapes
export const DEFAULT_STROKE_COLOR = "black";
export const DEFAULT_FILL_COLOR = "transparent";
export const DEFAULT_STROKE_WIDTH = 2;

export const DEFAULT_BACKGROUND_COLOR = "#ffffff";

// Tipos de herramientas disponibles
export const TOOL_TYPES = [
  "select",
  "line",
  "rect",
  "circle",
  "free",
  "text",
  "image",
  "marker",
];
